# conf/benchmark/musique.yaml
defaults:
  - default
  - _self_

name: "musique"

data:
  data_dir: "/mnt/project_rlinf/zhangruize/data/Asearcher-test-data/Musique_rand1000"
  metadata_file: "standardized_data.jsonl"
  field_mapping:
    task_id_field: "task_id"
    task_question_field: "task_question"
    ground_truth_field: "ground_truth"
    file_name_field: null

execution:
  max_tasks: null
  max_concurrent: 5
  pass_at_k: 1
